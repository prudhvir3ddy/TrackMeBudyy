<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_credentials"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="in.errorlabs.tmbtracker.Activities.Credentials">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_vertical">
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter the mobile number of to track"
        android:id="@+id/mobile"
        android:inputType="number"
        android:maxLength="10"
        android:textStyle="bold"
        />
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/passcode"
        android:hint="Enter the passcode to track"
        android:layout_below="@+id/mobile"
        android:textStyle="bold"
        android:inputType="number"
        android:maxLength="4"
        android:layout_marginTop="5dp"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/side_nav_bar"
        android:text="Start tracking"
        android:textColor="#ffffff"
        android:id="@+id/startbtn"
        android:layout_below="@+id/passcode"
        android:layout_marginTop="10dp"/>

</RelativeLayout>
</RelativeLayout>